---
applications:
- name: audio-recognition
  random-route: true
  path: ./
  memory: 128M
  buildpacks: 
  - python_buildpack
  build-parameters:
  before-all:
    - builder: custom
      commands:
        pip install -r requirements.txt
  command: python app.py
